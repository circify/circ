def main(public field[3][3] A, public field[3][3] B) -> field[3][3]:
   field[3][3] C = [[0; 3]; 3]
   for field i in 0..3 do
   for field j in 0..3 do
   for field k in 0..3 do
       C[i][j] = C[i][j] + A[i][k] * B[k][j]
   endfor
   endfor
   endfor
   return C
