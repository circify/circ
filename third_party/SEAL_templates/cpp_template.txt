#include "examples.h"

using namespace std;
using namespace seal;

Ciphertext server(SEALContext context, PublicKey public_key{header_inputs}){
    Evaluator evaluator(context);
    Encryptor encrypto(context, public_key);
    Plaintext plain_one("1");
    {server}
}

int main({main_inputs}) {
    {setup}

    Ciphertext output_encrypted = server(context, public_key{call_inputs});

    Plaintext output_plain;
    decryptor.decrypt(output_encrypted, output_plain);

    {post_computation}
    
    
    return 0;
}