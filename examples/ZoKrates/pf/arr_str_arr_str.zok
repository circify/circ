struct Pt {
    field x
    field y
}
struct Pts {
    Pt[2] pts
}

def main(private field y) -> field:
    Pt p = Pt {x: y, y: y}
    Pts[1] pts = [Pts { pts: [p, p] }]
    return pts[0].pts[0].y * pts[0].pts[1].x

